package com.still.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Calm Luxury — Core Palette ───────────────────────────────────────────────

// Backgrounds
val DeepDark = Color(0xFF111214)          // Main background — dark
val SurfaceDark = Color(0xFF1A1C1F)       // Card / sheet surface — dark
val SurfaceVariantDark = Color(0xFF222428) // Elevated surface — dark

// Accent — mat altın
val CalmGold = Color(0xFFB8A369)
val CalmGoldDim = Color(0xFF8A7A4E)       // Muted gold for dividers / icons
val CalmGoldSubtle = Color(0x1AB8A369)    // Gold with 10% opacity — highlights

// Text — dark theme
val OnBackgroundDark = Color(0xFFE8E2D9)  // Primary text
val OnSurfaceDark = Color(0xFFE8E2D9)
val OnSurfaceVariantDark = Color(0xFF9E9890) // Secondary / hint text

// Outline
val OutlineDark = Color(0xFF2E3035)       // Subtle divider
val OutlineVariantDark = Color(0xFF1E2025)

// Error
val ErrorDark = Color(0xFFCF6679)
val OnErrorDark = Color(0xFF1A0A0E)

// ─── Calm Luxury — Light Palette ──────────────────────────────────────────────

val BackgroundLight = Color(0xFFF8F5F0)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFF0EDE8)

val OnBackgroundLight = Color(0xFF1A1A1A)
val OnSurfaceLight = Color(0xFF1A1A1A)
val OnSurfaceVariantLight = Color(0xFF6B6560)

val OutlineLight = Color(0xFFD8D3CC)
val OutlineVariantLight = Color(0xFFEBE7E1)

val ErrorLight = Color(0xFFB00020)
val OnErrorLight = Color(0xFFFFFFFF)